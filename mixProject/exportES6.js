const pizzaStock = {
	papperoni : 10,
	sausage : 4,
	'double cheese' : 0
}

const isOvenReady = false;

const createPizza = (qty)=>console.log(`create ${qty} slice of pizza`);

export {pizzaStock, isOvenReady, createPizza};